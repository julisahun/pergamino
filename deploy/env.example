# /opt/dm-app/.env — mode 600. No secret lives here: a campaign's DM secret is
# minted when its console registers it and kept in that folder's
# .pergamino/campaign.json; the players' link secret is a row beside it.
DM_PORT=8085
DM_DB=/opt/dm-app/data/dm.sqlite
PUBLIC_URL=https://dm.sigint-pm.uk
