# /opt/dm-app/.env — mode 600. The server refuses to start without DM_TOKEN.
DM_TOKEN=change-me            # openssl rand -base64 32
DM_PORT=8085
DM_DB=/opt/dm-app/data/dm.sqlite
PUBLIC_URL=https://dm.sigint-pm.uk
